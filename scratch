
TODO:

* main game logic, Game class
	function process_state(time, events)
	state machine, states:
		STATE_DRAWING,
		STATE_RESULT,
	newimage() function to load an image, draw it somewhere on the screen
	main game loop waits for input,
	keypressed() function takes a pressed key, if it's 

* DONE print newgame screen
* add the bing image load library
* add a new state for image loading, the completion of this stage is the start-game trigger
* add controls to the display screen.
